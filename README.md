# web-gpu-glyph

Веб-приложение для рендеринга TrueType шрифтов с использованием WebGPU. Проект включает в себя полноценный парсер TTF файлов и систему визуализации текста в виде wireframe-контуров.

## 🚀 Возможности

- **Парсинг TrueType шрифтов**: Полноценный парсер TTF с поддержкой простых и составных (compound) глифов
- **WebGPU рендеринг**: Высокопроизводительный рендеринг контуров шрифтов с использованием WebGPU
- **Кэширование глифов**: Оптимизированное кэширование мешей глифов для быстрого рендеринга
- **Поддержка Unicode**: Маппинг Unicode символов в глифы через cmap таблицы (форматы 4 и 12)
- **Метрики шрифтов**: Полная поддержка горизонтальных метрик (advance width, LSB)

## 📋 Требования

- [Bun](https://bun.sh) (runtime и пакетный менеджер)
- Браузер с поддержкой WebGPU (Chrome 113+, Edge 113+, Safari 18+)
- TypeScript 5+

## 🛠️ Установка

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd everywhere-everything

# Установите зависимости
bun install
```

## 🎮 Запуск

```bash
# Запуск dev-сервера с hot reload
bun run dev
```

Сервер запустится на `http://localhost:3000` (или другом порту, указанном Bun).

## 📁 Структура проекта

```
everywhere-everything/
├── index.ts              # Основной файл рендеринга текста
├── point.ts              # Демо с интерактивной точкой и ореолом
├── TrueTypeFont.ts       # Парсер TrueType шрифтов
├── text.wgsl             # WGSL шейдеры для рендеринга текста
├── point.wgsl            # WGSL шейдеры для демо с точкой
├── server.ts             # HTTP сервер на Bun
├── index.html            # HTML шаблон
├── JetBrainsMono-Bold.ttf # Тестовый шрифт
└── package.json          # Конфигурация проекта
```

## 📚 API Документация

### `TrueTypeFont`

Основной класс для работы с TrueType шрифтами.

#### Статические методы

##### `TrueTypeFont.fromUrl(url: string): Promise<TrueTypeFont>`

Загружает шрифт из URL.

```typescript
const font = await TrueTypeFont.fromUrl("JetBrainsMono-Bold.ttf")
```

#### Свойства

- `unitsPerEm: number` - Количество единиц в em-пространстве шрифта
- `numGlyphs: number` - Общее количество глифов в шрифте
- `lineMetrics` - Линейные метрики шрифта:
  ```typescript
  {
    ascent: number    // Высота подъема
    descent: number   // Глубина спуска
    lineGap: number   // Межстрочный интервал
    unitsPerEm: number
  }
  ```

#### Методы

##### `mapCharToGlyph(codepoint: number): number`

Преобразует Unicode кодпоинт в идентификатор глифа.

```typescript
const gid = font.mapCharToGlyph("A".codePointAt(0)!)
```

##### `mapStringToGlyphs(str: string): number[]`

Преобразует строку в массив идентификаторов глифов.

```typescript
const glyphIds = font.mapStringToGlyphs("Hello")
```

##### `getGlyphOutline(gid: number): Outline`

Получает контур глифа с полной информацией о точках.

```typescript
const outline = font.getGlyphOutline(gid)
// outline.points: Float32Array - координаты [x0, y0, x1, y1, ...]
// outline.onCurve: Uint8Array - флаги on-curve точек
// outline.contours: Uint16Array - индексы последних точек контуров
```

##### `getGlyphPoints(gid: number): Float32Array`

Упрощенный метод, возвращающий только координаты точек.

##### `getHMetric(gid: number): { advanceWidth: number; lsb: number }`

Получает горизонтальные метрики глифа:
- `advanceWidth` - ширина advance в font-units
- `lsb` - left side bearing в font-units

## 🎨 Примеры использования

### Базовый рендеринг текста

```typescript
import { TrueTypeFont } from "./TrueTypeFont"

// Инициализация WebGPU
const adapter = await navigator.gpu.requestAdapter()
const device = await adapter.requestDevice()
const canvas = document.getElementById("canvas") as HTMLCanvasElement
const context = canvas.getContext("webgpu")!
context.configure({ device, format: navigator.gpu.getPreferredCanvasFormat() })

// Загрузка шрифта
const font = await TrueTypeFont.fromUrl("JetBrainsMono-Bold.ttf")

// Настройка параметров
const fontSizePx = 96
const scale = fontSizePx / font.unitsPerEm

// Рендеринг строки
const text = "Hello, World!"
for (const char of text) {
  const gid = font.mapCharToGlyph(char.codePointAt(0)!)
  const outline = font.getGlyphOutline(gid)
  const { advanceWidth } = font.getHMetric(gid)
  
  // ... рендеринг контура глифа
}
```

### Работа с метриками

```typescript
const font = await TrueTypeFont.fromUrl("font.ttf")

// Получение метрик строки
const metrics = font.lineMetrics
console.log(`Ascent: ${metrics.ascent}, Descent: ${metrics.descent}`)

// Вычисление высоты строки
const lineHeight = metrics.ascent - metrics.descent + metrics.lineGap
```

## 🔧 Архитектура

### Парсинг шрифтов

`TrueTypeFont` использует низкоуровневый парсинг TTF файлов через `DataView`:

1. **Табличный каталог**: Чтение структуры таблиц шрифта
2. **cmap**: Маппинг Unicode → Glyph ID (поддержка форматов 4 и 12)
3. **glyf/loca**: Извлечение контуров глифов (simple и compound)
4. **hhea/hmtx**: Горизонтальные метрики
5. **head/maxp**: Метаданные шрифта

### Рендеринг

1. **Извлечение контуров**: Получение on-curve точек из глифов
2. **Кэширование**: Создание GPU буферов для каждого глифа
3. **Трансформация**: Преобразование font-units → pixels → clip space
4. **Отрисовка**: Рендеринг через WebGPU pipeline с топологией `line-list`

### Оптимизации

- **Кэш глифов**: Меши глифов кэшируются после первого создания
- **Буферы GPU**: Вершинные и индексные буферы создаются один раз
- **Batch рендеринг**: Все глифы отрисовываются в одном render pass

## 🎯 Особенности реализации

### Поддержка составных глифов

Проект полностью поддерживает compound глифы (глифы, состоящие из других глифов):
- Трансформации компонентов (масштаб, поворот, сдвиг)
- Выравнивание по точкам или координатам
- Рекурсивная обработка вложенных компонентов

### Фильтрация on-curve точек

Для упрощения визуализации используются только on-curve точки, что создает wireframe-эффект вместо полных кривых Безье.

### Координатные системы

- **Font Units**: Внутренние единицы шрифта (зависят от `unitsPerEm`)
- **Pixels**: Экранные координаты
- **Clip Space**: Нормализованные координаты WebGPU (-1..1)

## 🐛 Известные ограничения

- Поддерживаются только cmap форматы 4 и 12
- Инструкции TrueType (hinting) игнорируются
- Вертикальные метрики (vhea/vmtx) не реализованы
- Kerning не поддерживается

## 📝 Лицензия

Проект находится в разработке.

## 👤 Автор

**zavx0z**

---

## 🔗 Полезные ссылки

- [WebGPU Specification](https://www.w3.org/TR/webgpu/)
- [TrueType Font Specification](https://developer.apple.com/fonts/TrueType-Reference-Manual/)
- [Bun Documentation](https://bun.sh/docs)
